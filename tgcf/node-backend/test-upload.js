const express = require( express); const app = express(); const multer = require(multer); const path = require(path); const fs = require(fs); const storage = multer.diskStorage({ destination: function (req, file, cb) { const uploadDir = path.join(__dirname, uploads); if (!fs.existsSync(uploadDir)) { fs.mkdirSync(uploadDir, { recursive: true }); } cb(null, uploadDir); }, filename: function (req, file, cb) { const uniqueSuffix = Date.now() + - + Math.round(Math.random() * 1E9); cb(null, uniqueSuffix + path.extname(file.originalname)); } }); const upload = multer({ storage: storage }); console.log(Multer初始化成功); console.log(Upload对象:, upload); console.log(Upload.single:, upload.single);
